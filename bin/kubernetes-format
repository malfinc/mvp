#!/usr/bin/env ruby
require "yaml"

Dir["kubernetes/development/**/*.yaml"].
  map{|path| [path, File.read(path)]}.
  map{|(path, content)| [path, YAML.load(content)]}.
  map{|(path, configuration)| [path, YAML.dump(configuration)]}.
  each{|(path, content)| File.write(path, content)}
