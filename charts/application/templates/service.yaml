apiVersion: v1
kind: Service
metadata:
  name: {{ include "blank-api-rails.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "blank-api-rails.name" . }}
    helm.sh/chart: {{ include "blank-api-rails.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "blank-api-rails.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
