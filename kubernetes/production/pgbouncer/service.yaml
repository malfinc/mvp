---
apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  labels:
    group: pgbouncer
    role: load-balancer
spec:
  type: ClusterIP
  selector:
    group: pgbouncer
  ports:
  - name: pgbouncer-postgres
    port: 5432
    targetPort: 5432
