apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sentry-data
  labels:
    group: sentry
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
