---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresql-origin-data
  labels:
    group: postgresql-origin
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
