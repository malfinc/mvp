---
apiVersion: v1
kind: Service
metadata:
  name: grafana
  labels:
    service: grafana
    role: browser-dashboard
    access: external
spec:
  type: ClusterIP
  ports:
    -
      name: http
      port: 80
      targetPort: 3000
  selector:
    service: grafana
    role: browser-dashboard
