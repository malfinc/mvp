---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-origin-data
  labels:
    group: redis-origin
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
