---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couchdb-origin-data
  labels:
    group: couchdb-origin
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi
